
name: Release PWA

on:
  push:
    tags: [ "v*" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Frontend-PWA
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # ⚡ CACHING: Makes the release build faster too
      - name: Setup Node.js (with Cache)
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './Frontend-PWA/package-lock.json'

      # ⚡ OPTIMIZED INSTALL
      - name: Install Dependencies
        run: npm install --prefer-offline --no-audit --no-fund

      # 🛡️ AUTOMATED TESTING for Releases
      - name: Run Unit Tests
        run: npm test -- run

      - name: Build App
        run: npm run build
        env:
          VITE_GAS_URL: ${{ secrets.VITE_GAS_URL }}

      - name: Zip Build Assets
        run: |
          cd dist
          zip -r ../clash-manager-pwa.zip .
          cd ..
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: Frontend-PWA/clash-manager-pwa.zip
          generate_release_notes: true
          draft: false
          prerelease: false
          body: |
            ## 🚀 PWA Update
            **Version**: ${{ github.ref_name }}
            
            Access the web application here:
            👉 **https://albidr.github.io/Clash-Manager/**
